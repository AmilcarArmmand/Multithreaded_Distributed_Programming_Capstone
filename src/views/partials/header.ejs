<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | CSC563 Capstone Project</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/"><%= typeof projectName !== 'undefined' ? projectName : 'CSC563 Capstone Project' %></a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <div class="navbar-nav me-auto">
                    <% if (locals.isAuthenticated) { %>
                        <a class="nav-link" href="/dashboard">Dashboard</a>
                        <a class="nav-link" href="/features">Features</a>
                        <a class="nav-link" href="/analytics">Analytics</a>
                    <% } %>
                </div>

                <div class="navbar-nav ms-auto">
                    <% if (locals.isAuthenticated && locals.user) { %>
                        <div class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown">
                                <img src="<%= user.picture %>" alt="Profile" class="rounded-circle me-2" width="24" height="24">
                                <%= user.firstName %>
                            </a>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="/dashboard">Dashboard</a></li>
                                <li><a class="dropdown-item" href="/dashboard/profile">Profile</a></li>
                                <li><a class="dropdown-item" href="/dashboard/settings">Settings</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/auth/logout">Sign Out</a></li>
                            </ul>
                        </div>
                    <% } else { %>
                        <a class="nav-link btn btn-outline-light ms-2" href="/auth/google">
                            <i class="fab fa-google me-1"></i> Sign In
                        </a>
                    <% } %>
                </div>
            </div>
        </div>
    </nav>
    <main class="container mt-4">
