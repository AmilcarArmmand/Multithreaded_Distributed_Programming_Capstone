// Central export for all models

import User from './schemas/User.js';
import ProjectData from './schemas/ProjectData.js';

export {
    User,
    ProjectData
};

// Default export for convenience
export default {
    User,
    ProjectData
};
